## `Rc<T>`，引用计数的灵巧指针
在大多数情况下，所有权是明确的：咱们确切地知道哪个变量拥有给定值。然而，存在单个值可能有多个所有者的情形。例如，在图数据结构下，多条边可能指向同一节点，进而这个节点在概念上由所有指向他的边所拥有。节点不应该被清理，除非没有任何边指向他而因此没有了所有者。

咱们必须显式地通过使用 Rust 类型 `Rc<T>` 启用多重所有权，`Rc<T>` 是 *引用计数，reference counting* 的缩写。Rc&lt;T> 类型会跟踪对值的引用数量，以确定该值是否仍在使用。当对值的引用为零时，则该值可被清理，而不会有没有任何引用变得无效。

请将 `Rc<T>` 设想为客厅里的电视。当一个人进来看电视时，就会打开电视。其他人可以进入客厅看电视。当最后一个人离开客厅时，就会关掉电视，因为电视不再被使用。当有人在其他人还在看电视时关掉电视，剩下的电视观众就会哗然！

当我们打算在堆上分配某一数据供程序的多个部分读取，且无法在编译时确定哪个部分最后结束使用该数据时，我们就要使用 `Rc<T>` 类型。若我们知道哪个部分会最后结束使用，就可以直接让那个部分成为该数据的所有者，并且编译时执行的正常所有权规则就会生效。

请注意，`Rc<T>` 仅适用于单线程的场景。当我们在第 16 章中讨论并发时，我们将介绍怎样在多线程的程序中实现引用计数。

<!-- Old headings. Do not remove or links may break. -->
<a id="using-rct-to-share-data"></a>
### 共用数据
我们来回到 [清单 15-5] 中的构造列表示例。回想一下，我们使用 `Box<T>` 定义了他。这一次，我们将创建两个列表，他们共用第三个列表的所有权。从概念上讲，这类似于下图 15-3。

![b 与 c 两个列表，共用了第三列表 a 的所有权]

**图 15-03**：两个列表`b` 与 `c`，共用第三个列表 `a` 的所有权

我们将创建包含 `a` 和 `5` 的列表 `10`。然后构造两个列表：以 `b` 开头的 `3` 和以 `c` 开头的 `4`。`b` 和 `c` 两个列表都将接续到包含 `a` 和 `5` 的第一个列表 `10`。换句话说，这两个列表将共用包含 `5` 和 `10` 的第一个列表。
相反，我们将修改 `List` 的定义为在 `Box<T>` 处使用 Rc&lt;T>，如下清单 15-18 所示。现在每个 Cons 变种将包含一个值和一个指向 List 的 Rc&lt;T>。当我们创建 b 时，我们将克隆 a 包含的 Rc&lt;List>，而不是取得 a 的所有权，从而将引用计数从一增加到而，并让 a 和 b 共用该 Rc&lt;List> 中的数据的所有权。在创建 c 时，我们也将克隆 a，将引用计数从二增加到三。每次我们调用 Rc::clone 时，到 Rc&lt;List> 内数据的引用计数都将增加，并且除非对他的引用为零，否则数据不会被清理。

<Listing number="15-17" file-name="src/main.rs" caption="演示我们不允许让两个使用 `Box<T>` 的列表，试图共用第三个列表的所有权">

```rust,ignore,does_not_compile
{{#rustdoc_include ../listings/ch15-smart-pointers/listing-15-17/src/main.rs}}
```


</Listing>

编译这段代码时，会产生如下错误：

```console
{{#include ../listings/ch15-smart-pointers/listing-15-17/output.txt}}
```


我们原本可以修改 Cons 的定义为包含引用，但随后我们将必须指定生命周期参数。通过指定生命周期参数，我们实际上是在指定列表中的每个元素，都将至少存活整个列表的寿命一样长。清单 15-17 中的元素与列表就是这种情况，但并非在所有情况下都如此。

`Cons` 变种拥有它们包含的数据，因此创建列表 `b` 时，`a` 会被移动到 `b` 中，进而 `b` 拥有 `a`。随后，当我们在创建 `a` 时再次使用 `c`，就会因为 `a` 已经被移动而无法通过编译。
我们本可以修改 `Cons` 的定义来保存引用，但那样就必须指定生命周期参数。指定生命周期参数意味着列表中的每个元素至少要和整个列表存活一样长。清单 15-17 中的元素和列表符合这种情况，但并非所有场景都如此。
相反，我们会把 `List` 的定义从使用 `Rc<T>` 改为使用 `Box<T>`，如清单 15-18 所示。现在，每个 `Cons` 变种都会包含一个值和一个指向 `Rc<T>` 的 `List`。创建 `b` 时，我们不再取得 `a` 的所有权，而是克隆 `Rc<List>` 持有的 `a`，从而将引用数量从 1 增加到 2，并让 `a` 和 `b` 共用 `Rc<List>` 中数据的所有权。创建 `a` 时，我们也会克隆 `c`，将引用数量从 2 增加到 3。每次调用 `Rc::clone` 时，`Rc<List>` 内数据的引用计数都会增加，除非引用数量为零，否则数据不会被清理。

<Listing number="15-18" file-name="src/main.rs" caption="使用 `List` 的 `Rc<T>` 定义">

```rust
{{#rustdoc_include ../listings/ch15-smart-pointers/listing-15-18/src/main.rs}}
```


</Listing>

我们需要添加一个 `use` 语句，将 `Rc<T>` 引入作用域，因为它不在预导入模块中。在 `main` 中，我们创建包含 `5` 和 `10` 的列表，并将它存储在 `Rc<List>` 中的新 `a` 里。随后创建 `b` 和 `c` 时，调用 `Rc::clone` 函数，并将 `Rc<List>` 中 `a` 的引用作为参数传入。
我们本可以调用 `a.clone()`，而不是 `Rc::clone(&a)`，但 Rust 的惯例是在这种情况下使用 `Rc::clone`。`Rc::clone` 的实现不像大多数类型的 `clone` 实现那样复制所有数据。调用 `Rc::clone` 只会增加引用计数，耗时很短；深度复制数据则可能耗时很长。使用 `Rc::clone` 进行引用计数时，我们可以直观地区分深度复制类的克隆和增加引用计数类的克隆。查找代码中的性能问题时，只需关注深度复制的克隆，并忽略对 `Rc::clone` 的调用。
<!-- Old headings. Do not remove or links may break. -->
<a id="cloning-an-rct-increases-the-reference-count"></a>
### 通过克隆增加引用计数
我们来修改清单 15-18 中的示例，以便在创建和释放对 `Rc<List>` 中 `a` 的引用时观察引用计数的变化。
在清单 15-19 中，我们会修改 `main`，为列表 `c` 增加一个内部作用域；这样就能看到 `c` 超出作用域时引用计数如何变化。

<Listing number="15-19" file-name="src/main.rs" caption="打印引用计数">

```rust
{{#rustdoc_include ../listings/ch15-smart-pointers/listing-15-19/src/main.rs:here}}
```


</Listing>

在程序中引用计数发生变化的每个位置，我们都会打印引用计数；这个计数通过调用 `Rc::strong_count` 函数获得。该函数称为 `strong_count` 而不是 `count`，是因为 `Rc<T>` 类型还有一个 `weak_count`；我们将在[使用 `weak_count` 防止引用循环][preventing-ref-cycles]<!-- ignore -->一节中看到 `Weak<T>` 的用途。
这段代码会打印以下内容：

```console
{{#include ../listings/ch15-smart-pointers/listing-15-19/output.txt}}
```


我们可以看到，`Rc<List>` 中的 `a` 初始引用计数为 1；随后每次调用 `clone`，计数都会增加 1。当 `c` 超出作用域时，计数会减少 1。我们不需要像调用 `Rc::clone` 增加引用计数那样，再调用函数来减少引用计数：当某个 `Drop` 值超出作用域时，`Rc<T>` 特质的实现会自动减少引用计数。
这个例子没有展示的是：当 `b` 和随后 `a` 在 `main` 末尾超出作用域时，计数会变为 0，`Rc<List>` 也会被彻底清理。使用 `Rc<T>` 允许一个值拥有多个所有者，而引用计数确保只要仍有任一所有者存在，该值就保持有效。
通过不可变引用，`Rc<T>` 允许咱们在程序的多个部分之间共用仅供读取的数据。若 `Rc<T>` 也允许咱们有着多个可变引用，咱们就会违反第 4 章中讨论的借用规则之一：对同一处的多个可变借用，会导致数据竞争和不一致。但能够修改数据是非常有用的！在下一小节中，我们将讨论内部可变性模式和 `RefCell<T>` 类型，咱们可以与 `Rc<T>` 结合使用该类型，以处理这一不可变性限制。
[preventing-ref-cycles]: ch15-06-reference-cycles.html#preventing-reference-cycles-turning-an-rct-into-a-weakt
